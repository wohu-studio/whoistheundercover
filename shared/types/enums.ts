export enum RoomStatus {
  WAITING = "WAITING",
  PLAYING = "PLAYING",
  RESULT = "RESULT",
}

export enum ReadyState {
  NOT_READY = "NOT_READY",
  READY = "READY",
}

export enum GameTypeEnum {
  UNDERCOVER = "UNDERCOVER",
  TASKWOLF = "TASKWOLF",
  COLOUR_HUNT = "COLOUR_HUNT",
}
